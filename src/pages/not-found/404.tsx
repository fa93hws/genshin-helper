import * as React from 'react';
import { Box, Typography } from '@material-ui/core';

export const NotFound = () => (
  <Box>
    <Typography variant="h4" gutterBottom>
      前面的区域以后再来探索吧
    </Typography>
    <Typography variant="body1">没有这个页面哦</Typography>
  </Box>
);
